name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps: 
      - name: Say Hello
        run: |
          echo "hello world"
      - name: Second Step
        run: |
          date
      - name : Directory Content
          run: |
            ls -l